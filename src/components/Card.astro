---
import Button from "./Button.astro";
const { heading, description, size } = Astro.props;
const cardStyling = "bg-yellow-200 hover:bg-yellow-100 active:bg-yellow-300 rounded-lg";
const sizeClasses = {
    "small": "w-[160px] lg:w-[340px]",
    "large": "w-[340px] lg:w-[420px]",
};
---
<div class:list={[cardStyling, sizeClasses[size]]}>
    <slot />
    <div class="px-3 py-2 lg:py-3">
        <h4 class="text-red-700 mb-1 lg:mb-3">{heading}</h4>
        <p class="text-xs md:text-sm lg:text-base mb-2 lg:mb-6">{description}</p>
        <Button variant="tertiary">
            <p>Read more</p>
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" class="w-5 h-5 md:w-6 md:h-6">
                <path fill-rule="evenodd" d="M6.22 4.22a.75.75 0 0 1 1.06 0l3.25 3.25a.75.75 0 0 1 0 1.06l-3.25 3.25a.75.75 0 0 1-1.06-1.06L8.94 8 6.22 5.28a.75.75 0 0 1 0-1.06Z" clip-rule="evenodd" />
            </svg>
        </Button>
    </div>
</div>