---
const { variant= "default" } = Astro.props;
const listStyling = "bg-yellow-100 active:bg-yellow-200 w-80 md:w-96 rounded-lg";
const variantClasses = {
    "1": "text-black-500",
    "2": "text-black-500",
    "cause": "",
};
---
<div class:list={[listStyling, variantClasses[variant]]}><slot /></div>